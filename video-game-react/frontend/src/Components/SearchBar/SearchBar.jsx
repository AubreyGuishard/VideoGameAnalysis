import React from 'react';

const SearchBar = ({}) => {
    return ( 
        <div>
            <input type="search" />
            <button>SEARCH</button>
        </div>
     );
}
 
export default SearchBar;